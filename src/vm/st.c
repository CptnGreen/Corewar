#include "corewar.h"

void    st(t_vm *vm, t_process *process)
{
    if (process + 1 = 50)
        process->registries[process + 3] = process->registries[process + 2]
    else if (process + 1 = 70)
       process->registries[process + ((process + 4) % IDX_MOD)] = process->registries[process + 2] 
}